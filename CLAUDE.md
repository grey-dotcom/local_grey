# Grey's Local Dev Environment

## 모델 설정
- 기본 모델: claude-sonnet-4-6 (속도 우선)

## 작업 범위 제한
- **허용 경로**: `/Users/grey/Desktop/local_grey/` 하위 디렉토리만 읽기/쓰기
- 이 경로 밖의 파일은 절대 수정하지 않는다
- 시스템 파일, 홈 디렉토리 루트, 기타 경로 접근 금지

## 프로젝트 구조
```
local_grey/
├── prompting/
│   ├── prompting-cli/     ← CLI 엔진 (수정 시 주의)
│   └── projects/          ← 실제 작업 프로젝트들
│       ├── app_screenshot/
│       ├── project-2/
│       └── project-3/
```

## 프로젝트 규칙
- 신규 프로젝트는 반드시 `local_grey/prompting/projects/` 하위에 생성
- 각 프로젝트 폴더에는 `context.js` 파일로 프로젝트 설명 작성
- 프로젝트 간 파일 교차 수정 금지

## 기술 스택
- **모바일**: React Native (TypeScript)
- **웹**: React / Next.js (TypeScript)
- TypeScript strict 모드 기본
- 컴포넌트는 함수형 + hooks

## 코딩 규칙
- 파일 수정 전 반드시 현재 내용 확인
- 대규모 변경 시 변경사항 먼저 설명 후 진행
- 한국어로 소통

## 필수 규칙: SVG 및 디자인 자산 분석 방식

> 모든 세션에서 지켜야 할 불변 규칙. 코드나 파일 작업에도 적용.

### 1. SVG 분석 원칙
- **SVG는 항상 플로우(상태 전이)** 의 관점으로 먼저 읽는다.
- 영제도 받을 때 “이 SVG들이 독립적인 케이스인가, 아니면 같은 카드의 상태 전이인가”를 **먼저 판단**한다.
- 동일한 구조에서 버튼만 다르면 → **UX 플로우**(before/after 또는 사용자 선택에 따른 상태 변화)
- 헤더 배지/레이아웃이 다르면 → **독립된 케이스** (다른 상황/상태)

### 2. 맥락 감안 역할 관점 적용
디자인 자산(SVG, 피그마, 스크린솟)을 분석할 때 다음 각 역할 관점을 사전에 적용한다:

| 역할 | 검토 항목 |
|---|---|
| **FE** | 코드 구현 가능성, 상태 관리 구조 |
| **디자인** | 비주얼 스펙(color, variant, spacing), 스타일 확정 여부 |
| **BE** | API 연동 필요 항목, 필드/상태 의존 여부 |
| **PM** | 서비스 정책 부합 여부, 미결 이슈 발생 여부 |

### 3. 주의 사항
- POLICY.md를 **우선 참조**. SVG 분석 전에 정책 문서가 없는지 확인.
- 답이 이미 POLICY에 있으면 다시 미결로 올리지 않는다.
- 미결이면 P/Q/B 항목으로 HANDOVER.md에 기록한다.

### 4. SVG 전달 규칙 (컨텍스트 초과 방지)
- **최대 3개/턴** 전달 원칙 (구조 파일로 저장 후 filesystem으로 읽는 방식 권장)
- 세션이 길면 SVG 전닯 전에 **새 세션 시작** 권장
